# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Streamlit
.streamlit/secrets.toml

# Data caches
.cache/
data_cache/
*.parquet
*.csv.gz

# OS / editor
.DS_Store
.idea/
.vscode/

# Paid scheme feed drops (PFF exports kept local, never committed — TOS).
# The free-source blend the update Action writes IS committed, so the deployed
# app can read it (see !coverage_*.csv below).
data/providers/dropbox/
scheme_data/*
!scheme_data/coverage_*.csv

# Live odds snapshots (local, not committed)
odds_snapshots/

# Evolution Engine: weekly snapshots ARE committed (they're the model's memory).
!history/
!history/*.csv
