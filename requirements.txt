# Community Cloud provisions Python 3.14 and installs prebuilt wheels for it, so
# we use floors (not ceilings) on the heavy compiled libs — capping them below the
# 3.14 wheels forces a from-source build that fails. Two guards are kept:
#   * pyarrow < 25  — 25.x has a known segfault (apache/arrow#50471); 24.x is safe.
#   * plotly  < 6   — hold the charting API on the 5.x the tabs were written for.
streamlit>=1.37
pandas>=2.0
numpy>=1.24
nfl_data_py==0.3.2
plotly>=5.20,<6
matplotlib>=3.7
requests>=2.31
lxml>=5.0
# Optional: only needed if a free stats site renders its tables with JavaScript.
# After installing, run once:  playwright install chromium
playwright>=1.44
pyarrow>=12,<25
